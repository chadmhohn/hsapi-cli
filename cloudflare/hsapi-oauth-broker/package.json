{
  "name": "@groundworkrevops/hsapi-oauth-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run types:check && npm run typecheck && npm test && npm run deploy:dry-run && npm run deploy:remote:dry-run",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "deploy:remote:dry-run": "wrangler deploy --dry-run --config wrangler.remote.jsonc --outdir .wrangler/dry-run-remote",
    "deploy:remote:production": "wrangler deploy --config wrangler.remote.operator.jsonc --env production",
    "deploy:remote:staging": "wrangler deploy --config wrangler.remote.operator.jsonc --env staging",
    "deploy:local:production": "wrangler deploy --config wrangler.operator.jsonc --env production",
    "deploy:local:staging": "wrangler deploy --config wrangler.operator.jsonc --env staging",
    "deploy:production": "wrangler deploy --config wrangler.operator.jsonc --env production",
    "deploy:staging": "wrangler deploy --config wrangler.operator.jsonc --env staging",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "types:check": "wrangler types --check"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "latest",
    "@types/node": "^26.1.1",
    "typescript": "latest",
    "vitest": "^4.1.0",
    "wrangler": "4.112.0"
  }
}
