{
  "_comment": "Portal-neutral hosted OAuth template. The CLI uses its bundled broker; HubSpot account selection binds the returned hub_id in this external token cache. Add portalId only to pin an expected account, or brokerUrl only to override the broker for a trusted private deployment.",
  "default": "oauth-hosted-example",
  "portals": {
    "oauth-hosted-example": {
      "label": "Example HubSpot Portal with Hosted OAuth",
      "baseUrl": "https://api.hubapi.com",
      "auth": {
        "defaultFamily": "oauth",
        "oauth": {
          "mode": "hosted_broker",
          "tokenCachePath": "~/.config/hsapi/oauth/oauth-hosted-example-token-cache.json"
        }
      }
    }
  }
}
